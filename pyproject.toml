[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dsa-playground"
version = "0.0.0"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["utils"]
